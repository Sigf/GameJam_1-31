using UnityEngine;
using System.Collections;


/*the mothership script will act as a queue for all enemies in a room/challenge
 * it's essentially acting as an event handler for all the enemies
 * I'm uncertain if this will actually be used, it depends on the optimization issues the AI may encounter
 * that's really the only purpose of this
 * */
public class mothership : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
